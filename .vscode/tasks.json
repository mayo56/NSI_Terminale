{
    "version": "2.0.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "GDB64",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "stopOnEntry": false,
            "preLaunchTask": "LINK"
        },
    
    ]
}